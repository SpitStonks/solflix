import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
  images: {
    domains: ['azure-sophisticated-bobolink-253.mypinata.cloud'],
  },
}

export default nextConfig;
